{
    auto_https off
}

:80 {
    handle /api/* {
        handle_path /api/* {
            reverse_proxy backend:4000
        }
    }

    handle {
        root * /usr/share/caddy
        file_server
        try_files {path} /index.html
    }

    log {
        output stdout
    }
}
